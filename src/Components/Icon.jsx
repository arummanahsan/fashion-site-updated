import React from 'react'
import { FaStar } from 'react-icons/fa'

const Icon = ({icon, className}) => {
  return (
    <FaStar className={`${className}`}>icon</FaStar>
  )
}

export default Icon